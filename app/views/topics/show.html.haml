%h2= @topic.title

%ol.breadcrumbs
  %li= link_to t(:community), '/forums'
  %li= @topic.forum.category.name
  %li= link_to @topic.forum.name, @topic.forum
  %li= @topic.title
  
  
%table.posts
  - @posts.each do |post|
    %tr
      %td
        hallo
      %td= raw post.to_html
      
/= will_paginate @posts